# deploy

The setup guide has moved to
[`docs/build-the-field-unit.md`](../docs/build-the-field-unit.md).

This directory holds the scripts that guide refers to, plus the systemd units they install.
Every script takes `--dry-run`.
