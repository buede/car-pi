# hardware

The full build guide has moved to
[`docs/build-the-can-interface.md`](../docs/build-the-can-interface.md).

This directory holds `carpi-can.net`, the netlist for the hand-built CAN interface. It is
checked pin by pin against the datasheets by `tests/test_hardware_netlist.py`.
