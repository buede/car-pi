"""car-pi: open-source vehicle diagnostics and coding hub.

Subpackages
-----------
core      transport, protocol, defs loading, rules evaluation
defs      the definition database (YAML data + JSON schemas)
sim       virtual ECU, so development does not require a car
cli       command-line surface
"""

__version__ = "0.0.1"
