# vehicles

How to contribute a profile is documented in
[`docs/contribute-vehicle-data.md`](../../../../docs/contribute-vehicle-data.md).

This directory holds `<make>/<platform>.yaml` files, validated against
[`../schema/vehicle.schema.json`](../schema/vehicle.schema.json). It is nearly empty on
purpose: nothing enters it that was not confirmed against a real car.
