# defs

The file formats are documented in
[`docs/definition-files.md`](../../../docs/definition-files.md).

This directory is the database itself: `schema/` holds the JSON Schemas enforced in CI,
`generic/` the standards-based definitions, and `vehicles/` the manufacturer-specific reads.
